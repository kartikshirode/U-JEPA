# Kaggle-only extras. Adds vLLM and any Linux-specific accelerators.
# Kaggle T4 / P100 sessions already ship with torch, so we let pip resolve.
-r requirements.txt
vllm>=0.6.6
flash-attn>=2.6.0; platform_system == "Linux"
